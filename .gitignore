# Session data
sessions/
projects/
shell-snapshots/
*.jsonl

# Cache & auth
cache/
mcp-needs-auth-cache.json
*.log

# Plugins (reinstall via marketplace)
plugins/

# Secrets / env
*.env
credentials.*
.credentials.json

# Local ephemeral data
backups/
file-history/
plans/
paste-cache/
session-env/
ide/
.last-cleanup

# Memory is a symlink to .ai-agents/memory (canonical)
memory
memory/
